<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirect any request beginning with /public to the application root
    RewriteCond %{REQUEST_URI} ^/public(/|$)
    RewriteRule ^public(/(.*))?$ /$2 [L,R=301,L]
</IfModule>
